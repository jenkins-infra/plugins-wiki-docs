This is a plugin which exports all kinds of information (like build job
status) using the openSCADA DA protocol. The protocol is using an event
 driven way instead of letting clients poll the recent status.
